name: CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3001457@ds.study.iitm.ac.in
        run: echo "Hello from Sanket!"